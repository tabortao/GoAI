services:
  goai:
    image: tabortoa/goai
    restart: always
    ports:
      - "1388:8080"
    env_file:
      - .env
    command: ["server"]
    volumes:
      - ./data:/app 
    #   - ollama

  # ollama:
  #   image: ollama/ollama
  #   ports: 
  #     - "11434:11434"
  #   volumes:
  #     - ollama_data:/root/.ollama
  #   container_name: ollama 

# volumes:
#   ollama_data: 